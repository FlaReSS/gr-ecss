# Copyright 2011,2012,2016,2018,2019 Free Software Foundation, Inc.
#
# This file was generated by gr_modtool, a tool from the GNU Radio framework
# This file is a part of gr-ecss
#
# SPDX-License-Identifier: GPL-3.0-or-later
#

########################################################################
# Setup library
########################################################################
include(GrPlatform) #define LIB_SUFFIX

list(APPEND ecss_sources
    agc_impl.cc
    pll_impl.cc
    coherent_phase_modulator_impl.cc
    phase_converter_impl.cc
    loop_filter_impl.cc
    gain_phase_accumulator_impl.cc
    signal_search_goertzel_impl.cc
#    signal_search_fft_v_impl.cc
    spl_encoder_impl.cc
    spl_decoder_impl.cc
    nrzl_encoder_impl.cc
    nrzl_encoder_subcarrier_impl.cc
    threshold_to_message_impl.cc )

set(ecss_sources "${ecss_sources}" PARENT_SCOPE)
if(NOT ecss_sources)
    MESSAGE(STATUS "No C++ sources... skipping lib/")
    return()
endif(NOT ecss_sources)

add_library(gnuradio-ecss SHARED ${ecss_sources})
target_link_libraries(gnuradio-ecss gnuradio::gnuradio-runtime)
target_include_directories(gnuradio-ecss
    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../include>
    PUBLIC $<INSTALL_INTERFACE:include>
  )
set_target_properties(gnuradio-ecss PROPERTIES DEFINE_SYMBOL "gnuradio_ecss_EXPORTS")

if(APPLE)
    set_target_properties(gnuradio-ecss PROPERTIES
        INSTALL_NAME_DIR "${CMAKE_INSTALL_PREFIX}/lib"
    )
endif(APPLE)

########################################################################
# Install built library files
########################################################################
include(GrMiscUtils)
GR_LIBRARY_FOO(gnuradio-ecss)

########################################################################
# Print summary
########################################################################
message(STATUS "Using install prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "Building for version: ${VERSION} / ${LIBVER}")

########################################################################
# Build and register unit test
########################################################################
include(GrTest)

# If your unit tests require special include paths, add them here
#include_directories()
# List all files that contain Boost.UTF unit tests here
list(APPEND test_ecss_sources
)
# Anything we need to link to for the unit tests go here
list(APPEND GR_TEST_TARGET_DEPS gnuradio-ecss)

if(NOT test_ecss_sources)
    MESSAGE(STATUS "No C++ unit tests... skipping")
    return()
endif(NOT test_ecss_sources)

foreach(qa_file ${test_ecss_sources})
    GR_ADD_CPP_TEST("ecss_${qa_file}"
        ${CMAKE_CURRENT_SOURCE_DIR}/${qa_file}
    )
endforeach(qa_file)
