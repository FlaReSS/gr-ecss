# auto-generated by grc.converter

id: ecss_pll
label: PLL
category: '[ecss]'

templates:
    imports: from gnuradio import ecss
    make: ecss.pll(${samp_rate}, ${N}, ${coefficients}, ${freq_central},${bw}, ${sel_loop_detector}, ${params_loop_detector})
    callbacks:
    - set_coefficients(${coefficients})
    - set_freq_central(${freq_central})
    - set_bw(${bw})

parameters:
-   id: samp_rate
    label: Sampling Rate
    dtype: int
    default: samp_rate
-   id: N
    label: N of bits
    dtype: int
    default: '38'
-   id: coefficients
    label: PLL Coefficients
    dtype: float_vector
    default: coefficients
-   id: freq_central
    label: Frequecy central
    dtype: real
    default: '0.0'
-   id: bw
    label: Bandwidth
    dtype: real
    default: '1000.0'
-   id: sel_loop_detector
    label: Loop Detector
    dtype: string
    default: 'External'
    options: ['ext', 'int']
    option_labels: [ 'External', 'Internal' ]
-   id: params_loop_detector
    label: Loop Detector Parameters
    dtype: float_vector
    default: ''
    
inputs:
-   domain: stream
    dtype: complex
-   domain: message
    id: lock_in_msg
    optional: true

outputs:
-   domain: stream
    dtype: complex
-   label: freq
    domain: stream
    dtype: float
    optional: true
-   label: phase error
    domain: stream
    dtype: float
    optional: true
-   label: phase delta
    domain: stream
    dtype: s64
    optional: true
-   domain: message
    id: lock_out_msg
    optional: true

asserts:
- ${ N >= 1 and N <= 52 }

file_format: 1
