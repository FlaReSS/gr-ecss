# auto-generated by grc.converter

id: ecss_pll
label: PLL
category: '[ecss]'
flags:

parameters:
-   id: samp_rate
    label: Sampling Rate
    dtype: int
    default: samp_rate
-   id: N
    label: N of bits
    dtype: int
    default: '38'
-   id: coefficients
    label: Coefficients
    dtype: float_vector
    default: coefficients
-   id: freq_central
    label: Frequecy central
    dtype: real
    default: '0.0'
-   id: bw
    label: Bandwidth
    dtype: real
    default: '1000.0'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex
-   label: freq
    domain: stream
    dtype: float
    optional: true
-   label: phase error
    domain: stream
    dtype: float
    optional: true
-   label: phase delta
    domain: stream
    dtype: s64
    optional: true
asserts:
- ${ N >= 1 and N <= 52 }

templates:
    imports: import ecss
    make: ecss.pll(${samp_rate}, ${N}, ${coefficients}, ${freq_central},${bw})
    callbacks:
    - set_coefficients(${coefficients})
    - set_freq_central(${freq_central})
    - set_bw(${bw})

file_format: 1
