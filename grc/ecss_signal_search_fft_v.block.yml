# auto-generated by grc.converter

id: ecss_signal_search_fft_v
label: Signal Search FFT
category: '[ecss]'

templates:
    imports: |-
        from gnuradio import ecss
        from gnuradio.fft import window
    make: ecss.signal_search_fft_v(${enable}, ${fftsize}, ${decimation}, ${average},
        ${win_type}, ${freq_central}, ${bandwidth}, ${freq_cutoff}, ${threshold}, ${samp_rate})
    callbacks:
    - set_freq_central(${freq_central})
    - set_bandwidth(${bandwidth})
    - set_freq_cutoff(${freq_cutoff})
    - set_threshold(${threshold})
    - set_average(${average})
    # - set_carrier(${carrier})
    - set_enable(${enable})

parameters:
-   id: enable
    label: Enable
    dtype: bool
    default: 'True'
-   id: win_type
    label: Window Type
    dtype: enum
    default: window.WIN_BLACKMAN_hARRIS
    options: [window.WIN_BLACKMAN_hARRIS, window.WIN_HAMMING, window.WIN_HANN, window.WIN_BLACKMAN,
        window.WIN_RECTANGULAR, window.WIN_KAISER, window.WIN_FLATTOP]
    option_labels: [Blackman-harris, Hamming, Hann, Blackman, Rectangular, Kaiser,
        Flat-top]
    hide: part
-   id: fftsize
    label: FFT size
    dtype: int
    default: '1024'
-   id: decimation
    label: Decimation
    dtype: int
    default: '1'
-   id: freq_central
    label: Central frequency of the signal
    dtype: real
    default: '0.0'
-   id: bandwidth
    label: Bandwidth of the signal
    dtype: real
    default: '2000.0'
-   id: average
    label: Average
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: ['Yes', 'No']
-   id: freq_cutoff
    label: Frequency cut-off (average)
    dtype: real
    default: '100.0'
-   id: threshold
    label: Threshold [dB]
    dtype: real
    default: '10.0'
-   id: samp_rate
    label: samp_rate
    dtype: real
    default: samp_rate

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

file_format: 1
