# auto-generated by grc.converter

id: ecss_spl_encoder
label: SP-L Encoder
category: '[ecss]'

templates:
    imports: from gnuradio import ecss
    make: ecss.spl_encoder(${oversampling})

parameters:
-   id: oversampling
    label: Oversampling
    dtype: int
    default: oversampling

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: float

file_format: 1
