# auto-generated by grc.converter

id: ecss_spl_encoder
label: SP-L Encoder
category: '[ecss]'

parameters:
-   id: samp_rate
    label: Samp Rate
    dtype: real
    default: samp_rate
-   id: bit_rate
    label: Bit Rate
    dtype: real
    default: bit_rate

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import ecss
    make: ecss.spl_encoder(${bit_rate}, ${samp_rate})

file_format: 1
