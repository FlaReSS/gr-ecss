# auto-generated by grc.converter

id: ecss_signal_search_goertzel
label: Signal Search Goertzel
category: '[ecss]'

parameters:
-   id: enable
    label: Enable
    dtype: bool
    default: 'True'
-   id: freq_central
    label: Central frequency of the signal
    dtype: real
    default: '0.0'
-   id: bandwidth
    label: Bandwidth of the signal
    dtype: real
    default: '2000.0'
-   id: average
    label: Average
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: ['Yes', 'No']
-   id: freq_cutoff
    label: Frequency cut-off (average)
    dtype: real
    default: '100.0'
-   id: threshold
    label: Threshold [dB]
    dtype: real
    default: '3.0'
-   id: samp_rate
    label: Samp Rate
    dtype: real
    default: samp_rate

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex
-   label: flag
    domain: stream
    dtype: byte
    optional: true

templates:
    imports: import ecss
    make: ecss.signal_search_goertzel(${enable}, ${average}, ${freq_central}, ${bandwidth},
        ${freq_cutoff}, ${threshold}, ${samp_rate})
    callbacks:
    - set_freq_central(${freq_central})
    - set_bandwidth(${bandwidth})
    - set_freq_cutoff(${freq_cutoff})
    - set_threshold(${threshold})
    - set_average(${average})
    - set_enable(${enable})

file_format: 1
