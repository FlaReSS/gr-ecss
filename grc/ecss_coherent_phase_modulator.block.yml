# auto-generated by grc.converter

id: ecss_coherent_phase_modulator
label: Coherent Phase Modulator
category: '[ecss]'

parameters:
-   id: N
    label: number of bits
    dtype: int
    default: '38'

inputs:
-   domain: stream
    dtype: s64

outputs:
-   domain: stream
    dtype: complex

asserts:
- ${ N >= 1 and N <= 52 }

templates:
    imports: import ecss
    make: ecss.coherent_phase_modulator(${N})

file_format: 1
