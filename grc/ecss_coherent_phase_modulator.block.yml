# auto-generated by grc.converter

id: ecss_coherent_phase_modulator
label: Coherent Phase Modulator
category: '[ecss]'

parameters:
-   id: N
    label: number of bits
    dtype: int
    default: '38'
-   id: n_inputs
    label: number of inputs
    dtype: int
    default: '1'
    hide: part

inputs:
-   domain: stream
    dtype: s64
    multiplicity: ${ n_inputs }
-   domain: message
    id: async_in
    optional: true

outputs:
-   domain: stream
    dtype: complex
asserts:
- ${ n_inputs >= 1 }
- ${ N >= 1 and N <= 52 }

templates:
    imports: import ecss
    make: ecss.coherent_phase_modulator(${N}, ${n_inputs})

file_format: 1
