# auto-generated by grc.converter

id: ecss_phase_converter
label: Phase Converter
category: '[ecss]'

parameters:
-   id: N
    label: number of bits
    dtype: int
    default: '38'

inputs:
-   domain: stream
    dtype: float

outputs:
-   domain: stream
    dtype: s64
asserts:
- ${ N >= 1 and N <= 52 }

templates:
    imports: import ecss
    make: ecss.phase_converter(${N})

file_format: 1
