# auto-generated by grc.converter

id: ecss_nrzl_encoder_subcarrier
label: NRZ-L Encoder with Sub-carrier
category: '[ecss]'

parameters:
-   id: sine
    label: Sub-carrier Signal
    dtype: bool
    default: 'True'
    options: ['True', 'False']
    option_labels: [Sine, Square]
-   id: freq_sub
    label: Sub-carrier frequency
    dtype: real
    default: '8000.0'
-   id: samp_rate
    label: Samp Rate
    dtype: real
    default: samp_rate
-   id: bit_rate
    label: Bit Rate
    dtype: real
    default: bit_rate

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import ecss
    make: ecss.nrzl_encoder_subcarrier(${sine}, ${freq_sub}, ${bit_rate}, ${samp_rate})

file_format: 1
