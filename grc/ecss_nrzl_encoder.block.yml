# auto-generated by grc.converter

id: ecss_nrzl_encoder
label: NRZ-L Encoder
category: '[ecss]'

templates:
    imports: from gnuradio import ecss
    make: ecss.nrzl_encoder(${bit_rate}, ${samp_rate})

parameters:
-   id: samp_rate
    label: Samp Rate
    dtype: real
    default: samp_rate
-   id: bit_rate
    label: Bit Rate
    dtype: real
    default: bit_rate

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: float

file_format: 1
